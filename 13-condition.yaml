- name: condition
  hosts: web 
  become: yes
  task:
  - name: check user 
    ansible.builtin.commend: id Nagarjuna
    register: USER
    ignore_errors: true
  
  - name: print user information 
    ansible.builtin.debug
      msg: "USER Info: {{USER}}"
    
  - name: say hellow
    ansible.builtin.debug
      msg: "Hellow"
